from pathlib import Path
# we'll use these constants for file paths in the data ingestion pipeline research/01_data_ingestion
CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")